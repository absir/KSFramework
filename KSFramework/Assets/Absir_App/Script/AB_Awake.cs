using UnityEngine;
using System.Collections;

public class AB_Awake : MonoBehaviour {

	void Awake()
	{
		Debug.Log ("AB_Awake " + name);
	}

	// Use this for initialization
	void Start () {
		Debug.Log ("AB_Start " + name);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
